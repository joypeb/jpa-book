server:
  servlet:
    encoding:
      force-response: true
  port: 8081
spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect